// Refactoring Exercise
// Example of poor naming that needs to be improved
function c(n) {
    return n * n;
}

// Improve the code with better naming
function calculateSquare(number) {
    return number * number;
}